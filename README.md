

### version 0.1.0

# Main Features
**Data extraction and functions**

- IP address information (`Telegram bots, python tools`)
- Email address information (Search all info by Email)
- Phone number information (Search all info by Phone)
- Hosts Search (finds the Hosts file if you are on Windows 10)
- Exploit CVE (Cve exploit :0)
- DNS lookup (checks the status of DNS servers on the PC)
- DNS reverse (DNS reverse)
- Telegram tools

<img width="1115" height="685" alt="image" src="https://github.com/user-attachments/assets/2095ef47-8240-4d1e-8dc0-e404fb007d0b" />

# How to dowload?

To download, follow the instructions here. Right-click ⮕ Open in the Terminal ⮕ and write the command

```
python -m pip install requests
python -m pip install dns
python -m pip install phonenumbers
```



# Warning

**Some functions are not fully completed yet. Everything will be done in the next updates.**
